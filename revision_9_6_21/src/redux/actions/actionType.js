import {ActionType } from "redux-promise-middleware";

export const actionType = {
    ui: {
        setLanguage: 'UI_SETLANGUAGE',
        showCamera: 'UI_SHOWCAMERA',
        setProfilePhoto: 'UI_SET'
    }
}